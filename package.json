{
  "private": true,
  "scripts": {
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "prepare": "husky",
    "release": "bumpp --tag 'napi-v%s' --commit 'release: napi v%s' napi/package.json",
    "dev:napi": "pnpm --filter typack run build-wasm && cp napi/typack.wasm repl/public/wasm/typack.wasm",
    "dev:vite": "pnpm --filter @typack/repl run dev",
    "repl:dev": "pnpm run dev:napi && (cargo watch -w src -w napi/src -s 'pnpm run dev:napi' & pnpm run dev:vite)"
  },
  "devDependencies": {
    "bumpp": "^10.4.1",
    "husky": "^9.1.7",
    "oxfmt": "latest"
  },
  "packageManager": "pnpm@10.30.3"
}
